[package]
name = "stratus-macros"
version = "0.1.0"
authors = ["alexa <alexa.griffin42@gmail.com>"]
edition = "2018"
description = "macros for stratus, probably should not be used on its own"
license = "MIT"

[lib]
proc-macro = true

[dependencies]
syn = { version = "1.0.8", features = ["full"] }